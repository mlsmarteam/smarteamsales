<% provide(:title, 'New') %>
<h1>New expenditure type</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  	<%= form_for(@expenditure_type) do |f| %>
  	<%= render 'shared/error_messages', object: f.object %>
  	<%= f.label :name %>
  	<%= f.text_field :name %>

  	<%= f.submit "Create expenditure type", class: "btn btn-primary" %>
  	<% end %>
  </div>
</div>