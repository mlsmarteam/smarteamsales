<% provide(:title, 'Edit') %>
<h1>Edit expenditure type</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  	<%= form_for(@expenditure_type) do |f| %>
  	<%= render 'shared/error_messages', object: f.object %>
  	<%= f.label :name %>
  	<%= f.text_field :name %>

  	<%= f.submit "Edit expenditure type", class: "btn btn-primary" %>
  	<% end %>
  </div>
</div>