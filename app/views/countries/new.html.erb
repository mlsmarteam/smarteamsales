<% provide(:title, 'New') %>
<h1>New country</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  	<%= form_for(@country) do |f| %>
  	<%= render 'shared/error_messages', object: f.object %>
  	<%= f.label :name %>
  	<%= f.text_field :name %>

  	<%= f.submit "Create country", class: "btn btn-primary" %>
  	<% end %>
  </div>
</div>